name: scrabble-score

dependencies:
  - base

library:
  exposed-modules: Scrabble
  source-dirs: src
  dependencies:

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - scrabble-score
      - hspec
