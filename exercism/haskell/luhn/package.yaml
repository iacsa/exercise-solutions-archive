name: luhn

dependencies:
  - base

library:
  exposed-modules: Luhn
  source-dirs: src
  dependencies:

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - luhn
      - hspec
